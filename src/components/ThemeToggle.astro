---
import { Icon } from "astro-icon/components";
---

<button id="theme-toggle" class="cursor-pointer rounded-md p-2 text-sm hover:bg-stone-200 d-hover transition-colors" aria-label="Toggle dark mode">
  <Icon id="sun-icon" name="mingcute:sun-line" size={20} class="hidden" aria-hidden="true" />
  <Icon id="moon-icon" name="mingcute:moon-line" size={20} class="hidden" aria-hidden="true" />
</button>

<script>
  const toggle = document.getElementById("theme-toggle");
  const sun = document.getElementById("sun-icon");
  const moon = document.getElementById("moon-icon");

  function applyTheme(theme) {
    if (theme === "dark") {
      document.documentElement.classList.add("dark");
      sun?.classList.remove("hidden");
      moon?.classList.add("hidden");
    } else {
      document.documentElement.classList.remove("dark");
      sun?.classList.add("hidden");
      moon?.classList.remove("hidden");
    }
  }

  // Init icon state
  const initial = localStorage.getItem("theme");
  if (initial === "dark" || (!initial && window.matchMedia("(prefers-color-scheme: dark)").matches)) {
    sun?.classList.remove("hidden");
  } else {
    moon?.classList.remove("hidden");
  }

  toggle?.addEventListener("click", () => {
    const isDark = document.documentElement.classList.contains("dark");
    const newTheme = isDark ? "light" : "dark";
    localStorage.setItem("theme", newTheme);
    applyTheme(newTheme);
  });
</script>